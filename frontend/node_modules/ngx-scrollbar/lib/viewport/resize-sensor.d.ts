import { Observable } from 'rxjs';
import { ScrollbarUpdateReason } from '../ng-scrollbar.model';
export declare function resizeSensor(element: HTMLElement, throttleDuration: number, isScrollbar?: boolean): Observable<ScrollbarUpdateReason>;
